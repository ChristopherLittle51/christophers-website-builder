services:
  builder:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      DATA_DIR: /app/data
      STORAGE_DRIVER: filesystem
    volumes:
      - builder-data:/app/data
    restart: unless-stopped

volumes:
  builder-data:
