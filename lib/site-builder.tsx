'use client';

// Compatibility facade: persisted Puck documents depend on the component keys
// assembled behind this stable import path.
export { builderConfig } from './site-builder/config';
export { starterData } from './templates';
